# Default JWT secret (replace in production or set via env var APP_JWT_SECRET)
# Must be at least 32 bytes for HS256. Replace with a secure value in production.
app.jwt.secret=Fv1s9Qm8Zt3pR6xYw4cV2nB0kL8jH5sD7gN6uP1qR2tY3uV4
# JWT expiration in milliseconds (1 hour)
app.jwt.expiration-ms=3600000

# MongoDB Atlas connection string provided by user
# NOTE: keep credentials secret; consider moving this to an environment variable in production.
spring.data.mongodb.uri=mongodb+srv://razvanhosu32_db_user:tHpEQITSTIBivqrG@cluster0.zsixano.mongodb.net/BoardShip?retryWrites=true&w=majority&appName=Cluster0

# Server port
server.port=8080
# Bind to all network interfaces so other LAN devices can reach the backend
server.address=0.0.0.0

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web.cors=DEBUG
logging.level.com.boardship.backend=DEBUG

# If you want to run without MongoDB during development, uncomment the following line to disable Mongo autoconfiguration
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.mongo.MongoAutoConfiguration,org.springframework.boot.autoconfigure.mongo.MongoReactiveAutoConfiguration
